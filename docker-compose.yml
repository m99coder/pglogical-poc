version: "3"
services:
  provider:
    build: ./provider
    environment:
      - POSTGRES_PASSWORD=s3cr3t
    networks:
      default:
        aliases:
          - pg_cluster
  subscriber:
    build: ./subscriber
    environment:
      - POSTGRES_PASSWORD=s3cr3t
    networks:
      default:
        aliases:
          - pg_cluster
